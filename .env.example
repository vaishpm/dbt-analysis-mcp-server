DBT_PROJECT_DIR=/absolute/path/to/your/dbt/project
DBT_PATH=dbt

# Optional
# DBT_PROFILES_DIR=/absolute/path/to/profiles/dir
# DBT_TARGET=prod
# DBT_DEFAULT_LIMIT=100
# DBT_COMMAND_TIMEOUT_SECONDS=120

# Disabled by default because dbt run can mutate warehouse state.
# ALLOW_DBT_RUN=true
